# eFlow Core IG

Cross-cutting definitions for the eFlow ecosystem (security/audit/error patterns and base operations).

## First build

1) Ensure Java is installed.
2) Run the build:

- ./_genonce.sh

This downloads the IG Publisher into tools/publisher/ (shared) on first run.
