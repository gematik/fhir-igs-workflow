# eFlow Server IG

Umbrella IG for the full server contract, CapabilityStatement, and conformance matrix. Depends on eFlow Core and all modules.

## First build

1) Ensure Java is installed.
2) Run the build:

- ./_genonce.sh

This downloads the IG Publisher into tools/publisher/ (shared) on first run.
